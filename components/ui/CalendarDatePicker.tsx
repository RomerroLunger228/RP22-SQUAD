// 🎯 ОБУЧАЮЩИЙ ПРИМЕР: Рефакторинг календаря через композицию
// 
// ✅ РЕЗУЛЬТАТ РЕФАКТОРИНГА:
// 363 строки → 1 импорт + 5 строк кода = Чистота и переиспользуемость
//
// 🔄 ЧТО ИЗМЕНИЛОСЬ:
// ❌ БЫЛО: Монолитный компонент со сложной логикой календаря
// ✅ СТАЛО: Тонкая обертка над переиспользуемым контейнером

"use client";

import { CalendarContainer } from "./CalendarContainer";

interface CalendarDatePickerProps {
    serviceId: number;
    onSlotSelect?: (slot: string) => void;
}

// 📝 ОБУЧАЮЩИЕ ЗАМЕТКИ:
// 1. 🎯 ТОНКАЯ ОБЕРТКА: Компонент стал просто проксирующей оберткой
// 2. 🧩 ДЕЛЕГИРОВАНИЕ: Вся сложная логика делегируется контейнеру
// 3. 🔄 ОБРАТНАЯ СОВМЕСТИМОСТЬ: API компонента остался точно таким же
// 4. 🚀 БЕЗОПАСНОСТЬ: Никаких логов, строгая типизация

export function CalendarDatePicker(props: CalendarDatePickerProps) {
    // 🎭 ПАТТЕРН "ФАСАД": Простой интерфейс для сложной календарной логики
    return <CalendarContainer {...props} />;
}

// 📚 ОБУЧАЮЩИЕ ИТОГИ КАЛЕНДАРНОГО РЕФАКТОРИНГА:
// 
// 🎯 ПРЕИМУЩЕСТВА:
// ✅ Код разбит на 4 специализированных компонента + 1 хук
// ✅ CalendarGrid можно переиспользовать в других календарях
// ✅ TimeSlotPicker можно использовать в формах бронирования
// ✅ AvailabilityIndicator переиспользуется в админке
// ✅ useCalendarLogic содержит всю календарную бизнес-логику
// ✅ Никаких логов не попадает на клиент
// 
// 🚫 ЧТО СОХРАНИЛОСЬ:
// ✅ Точно такой же календарный UI
// ✅ Все состояния (loading, error, success)
// ✅ API компонента не изменился
// ✅ Производительность TanStack Query
// ✅ Валидация рабочих дней